# table — columnar view of whitespace-delimited input (ps aux, ls -l, df, …).
#   ps aux | arb -p table
table .rows
source .rows { in }

test "passthrough rows unchanged" {
    given "USER PID CMD" "root 1 init"
    run { in }
    want "USER PID CMD" "root 1 init"
}
